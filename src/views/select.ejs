<!DOCTYPE html>
<html lang="zh-Hant-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>服務學習選擇系統</title>

    <!-- External Resources -->
    <base target="_top">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/select.css">
</head>

<body onload="LoadSelectBoxOption()">
<header>
    <h1>選擇服務學習</h1>
    <h3 id="username"><?= username ?></h3>
</header>

<div class="nav-container">
    <form method="post" action="<?= getUrl() ?>">
        <input type="hidden" value="<?= username ?>" name="username">
        <input type="hidden" value="<?= studentid ?>" name="studentid">
        <button type="submit" value="Home" name="HomeButton">
            <i class="fa fa-home"></i> 首頁
        </button>
    </form>

    <form method="post" action="<?= getUrl() ?>">
        <input type="hidden" value="<?= username ?>" name="username">
        <input type="hidden" value="<?= studentid ?>" name="studentid">
        <button type="submit" value="Profile" name="ProfileButton">
            <i class="fa fa-user"></i> 我的資訊
        </button>
    </form>

    <form method="post" action="<?= getUrl() ?>">
        <input type="hidden" value="<?= username ?>" name="username">
        <input type="hidden" value="<?= studentid ?>" name="studentid">
        <button type="submit" value="SelectService" name="SelectLearningButton">
            <i class="fa fa-hand-o-up"></i> 選擇服務學習
        </button>
    </form>

    <form method="post" action="<?= getUrl() ?>">
        <input type="hidden" value="<?= username ?>" name="username">
        <button type="submit" value="Logout" name="LogoutButton">
            <i class="fa fa-sign-out"></i> 登出
        </button>
    </form>
</div>

<div id="loading-spinner" class="loading-spinner">
    <div class="spinner"></div>
    <p>載入中...</p>
</div>

<div id="selectbox-container"></div>

<div class="info" data-selected="">
    <span class="loader">載入中...</span>
    <span class="no-selection">尚未選擇任何活動</span>
    <div class="event-info">
        <p id="event-name"></p>
        <p id="event-time"></p>
        <p id="event-place"></p>
        <p id="event-hours"></p>
        <p id="event-note"></p>
    </div>
</div>

<form method="post" action="">
    <button id="confirm-btn" type="button">確定</button>
</form>

<script src="/js/select.js" />
</body>
</html>